version: "3.1"
services:
    mysql:
      image: mysql:5.7
      container_name: titomiguelcosta-hammer-mysql
      working_dir: /application
      volumes:
        - .:/application
      environment:
        - MYSQL_ROOT_PASSWORD=galinha
        - MYSQL_DATABASE=hammer
        - MYSQL_USER=hammer
        - MYSQL_PASSWORD=galinha
      ports:
        - "8082:3306"

    elasticsearch:
      image: elasticsearch:5.4-alpine
      container_name: titomiguelcosta-hammer-elasticsearch

    webserver:
      image: nginx:alpine
      container_name: titomiguelcosta-hammer-webserver
      working_dir: /application
      volumes:
          - .:/application
          - ./docker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      ports:
       - "8080:80"

    php-fpm:
      build: docker/php-fpm
      container_name: titomiguelcosta-hammer-php-fpm
      working_dir: /application
      volumes:
        - .:/application
        - ./docker/php-fpm/php-ini-overrides.ini:/etc/php/7.2/fpm/conf.d/99-overrides.ini
